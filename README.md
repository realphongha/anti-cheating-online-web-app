# anti-cheating-online-web-app
Frontend web application for anti cheating online system

# How to install
Run ```yarn install```  
Run ```yarn start``` (for development mode) or ```yarn build``` then ```serve -s build``` (for production mode)
